{
  "name": "post-vote",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -p \"{pid}\" -c \"blue,red,green\" \"yarn workspace @post-vote/contract build\" \"yarn workspace @post-vote/server dev\" \"yarn workspace @post-vote/client dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.1"
  }
}